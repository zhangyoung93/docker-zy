# 后台启动：docker compose up -d

# 后台启动指定服务：docker compose up -d [service-name]

# 重建镜像后启动：docker compose up -d --build

# 停止但保留容器：docker compose stop

# 停止但保留指定容器：docker compose stop [service-name]

# 停止并删除容器，重建镜像用：docker compose down